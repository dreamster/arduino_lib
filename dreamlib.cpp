#define IMPORT_FROM_ARDUINO_LIBRARIES_FOLDER
 
#include "Dreamlib.h"

/**

 All of the code is embedded in the header file, in the classes themselves.
 
 This allows the library to be used just by including the .h file with your 
 project in the same folder as the .ino file, without installing it to the 
 Arduino libraries folder.
 
**/
